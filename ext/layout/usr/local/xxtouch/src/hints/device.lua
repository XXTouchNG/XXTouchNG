device = {}

-- $DOC_ROOT/Handbook/device/device.reset_idle.html
function device.reset_idle() end

-- $DOC_ROOT/Handbook/device/device.lock_screen.html
function device.lock_screen() end

-- $DOC_ROOT/Handbook/device/device.unlock_screen.html
function device.unlock_screen(password) end

-- $DOC_ROOT/Handbook/device/device.is_screen_locked.html
function device.is_screen_locked() end

-- $DOC_ROOT/Handbook/device/device.front_orien.html
function device.front_orien() end

-- $DOC_ROOT/Handbook/device/device.lock_orien.html
function device.lock_orien() end

-- $DOC_ROOT/Handbook/device/device.unlock_orien.html
function device.unlock_orien() end

-- $DOC_ROOT/Handbook/device/device.is_orien_locked.html
function device.is_orien_locked() end

-- $DOC_ROOT/Handbook/device/device.vibrator.html
function device.vibrator() end

-- $DOC_ROOT/Handbook/device/device.type.html
function device.type() end

-- $DOC_ROOT/Handbook/device/device.name.html
function device.name() end

-- $DOC_ROOT/Handbook/device/device.set_name.html
function device.set_name(name) end

-- $DOC_ROOT/Handbook/device/device.udid.html
function device.udid() end

-- $DOC_ROOT/Handbook/device/device.serial_number.html
function device.serial_number() end

-- $DOC_ROOT/Handbook/device/device.wifi_mac.html
function device.wifi_mac() end

-- $DOC_ROOT/Handbook/device/device.bluetooth_mac.html
function device.bluetooth_mac() end

-- $DOC_ROOT/Handbook/device/device.ifaddrs.html
function device.ifaddrs() end

-- $DOC_ROOT/Handbook/device/device.macaddrs.html
function device.macaddrs() end

-- $DOC_ROOT/Handbook/device/device.battery_level.html
function device.battery_level() end

-- $DOC_ROOT/Handbook/device/device.battery_state.html
function device.battery_state() end

-- $DOC_ROOT/Handbook/device/device.turn_on_wifi.html
function device.turn_on_wifi() end

-- $DOC_ROOT/Handbook/device/device.turn_off_wifi.html
function device.turn_off_wifi() end

-- $DOC_ROOT/Handbook/device/device.is_wifi_on.html
function device.is_wifi_on() end

-- $DOC_ROOT/Handbook/device/device.turn_on_data.html
function device.turn_on_data() end

-- $DOC_ROOT/Handbook/device/device.turn_off_data.html
function device.turn_off_data() end

-- $DOC_ROOT/Handbook/device/device.is_data_on.html
function device.is_data_on() end

-- $DOC_ROOT/Handbook/device/device.turn_on_bluetooth.html
function device.turn_on_bluetooth() end

-- $DOC_ROOT/Handbook/device/device.turn_off_bluetooth.html
function device.turn_off_bluetooth() end

-- $DOC_ROOT/Handbook/device/device.is_bluetooth_on.html
function device.is_bluetooth_on() end

-- $DOC_ROOT/Handbook/device/device.turn_on_airplane.html
function device.turn_on_airplane() end

-- $DOC_ROOT/Handbook/device/device.turn_off_airplane.html
function device.turn_off_airplane() end

-- $DOC_ROOT/Handbook/device/device.is_airplane_on.html
function device.is_airplane_on() end

-- $DOC_ROOT/Handbook/device/device.turn_on_vpn.html
function device.turn_on_vpn() end

-- $DOC_ROOT/Handbook/device/device.turn_off_vpn.html
function device.turn_off_vpn() end

-- $DOC_ROOT/Handbook/device/device.is_vpn_on.html
function device.is_vpn_on() end

-- $DOC_ROOT/Handbook/device/device.flash_on.html
function device.flash_on() end

-- $DOC_ROOT/Handbook/device/device.flash_off.html
function device.flash_off() end

-- $DOC_ROOT/Handbook/device/device.is_flash_on.html
function device.is_flash_on() end

-- $DOC_ROOT/Handbook/device/device.reduce_motion_on.html
function device.reduce_motion_on() end

-- $DOC_ROOT/Handbook/device/device.reduce_motion_off.html
function device.reduce_motion_off() end

-- $DOC_ROOT/Handbook/device/device.is_reduce_motion_on.html
function device.is_reduce_motion_on() end

-- $DOC_ROOT/Handbook/device/device.assistive_touch_on.html
function device.assistive_touch_on() end

-- $DOC_ROOT/Handbook/device/device.assistive_touch_off.html
function device.assistive_touch_off() end

-- $DOC_ROOT/Handbook/device/device.is_assistive_touch_on.html
function device.is_assistive_touch_on() end

-- $DOC_ROOT/Handbook/device/device.brightness.html
function device.brightness() end

-- $DOC_ROOT/Handbook/device/device.set_brightness.html
function device.set_brightness(brightness) end

-- $DOC_ROOT/Handbook/device/device.set_autolock_time.html
function device.set_autolock_time(time_minutes) end

-- $DOC_ROOT/Handbook/device/device.set_volume.html
function device.set_volume(volume) end

-- $DOC_ROOT/Handbook/device/device.join_wifi.html
function device.join_wifi(ssid, password, network_type) end